{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/public"
      }
    }
  ],
  "installCommand": "corepack prepare pnpm@10.4.1 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack prepare pnpm@10.4.1 --activate && pnpm build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
